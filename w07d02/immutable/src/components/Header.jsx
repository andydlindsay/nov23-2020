const Header = (props) => {
  // const arr = useState('jstamos');
  // const username = arr[0];
  // const setUsername = arr[1];
  // console.log(props);

  return (
    <div>
      <h2>{props.whatever}</h2>
    </div>
  );
};

export default Header;
